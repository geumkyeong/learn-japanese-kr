{
  "name": "learn-japanese-kr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "client-build": "cd client && npm ci && npm run build && cd ../",
    "server-build": "npm ci --include=dev",
    "vercel-build": "npm run client-build && npm run server-build && mv ./client/build ./build",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
